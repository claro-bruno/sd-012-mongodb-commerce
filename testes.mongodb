use("commerce");

db.produtos.updateMany(
  {},
  { $set: { avaliacao: NumberInt(0) } }
);

db.produtos.updateMany(
  { tags: { $elemMatch: { tags: "bovino" } } },
  { $max: { avaliacao: 5 } }
);

db.produtos.find();