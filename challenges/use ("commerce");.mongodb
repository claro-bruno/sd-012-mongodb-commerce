use ("commerce");
db.produtos.find();

use ("commerce");
db.produtos.updateMany(
  { tags: { $all: ["bovino", "pão"]}},
  { $inc: { "vendasPorDia.6": 120 } },
);

use ("commerce");
db.produtos.updateMany(
  {},
  { $push: {
    vendasPorDia: {
      $each: [0, 0, 0, 0, 0, 0, 0],
    },
  } },
);

use ("commerce");
db.produtos.find(
  {},
  {
  nome: 1,
  _id: 0,
  vendasPorDia: 1,
  },
);


