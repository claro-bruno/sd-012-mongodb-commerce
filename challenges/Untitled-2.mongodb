use ("commerce");

db.produtos.updateMany({}, 
{$unset: { ingredientes: "cebola"} });

db.produtos.find({});

db.produtos.updateMany(
    {},
    { $unset: { ingredientes: "cebola" } },
  );

  db.produtos.find({});
  db.produtos.find(
    {},
    { _id: 0, nome: 1, ingredientes: 1 },
  ); 

